trigger:
- main

pool:
  vmImage: windows-2022

schedules:
- cron: "45 13 10 6 * "
  displayName: Publish today at 13:45 PM UTC,
  branches:
    include:
    - main
  always: true

steps:
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      Write-Host "About to publish..."
       Write-Host "About to publishing..."
      #dotnet new tool-manifest
      #dotnet nuget add source -n Sitecore https://nuget.sitecore.com/resources/v3/index.json
      #dotnet tool install Sitecore.CLI   
      Write-Host "Publish completed"